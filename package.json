{
  "name": "kartoncss",
  "version": "1.0.5",
  "main": "karton.css",
  "scripts": {
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "minify karton.css > karton.min.css && git add . --all && git commit -m new && surge ./ && npm publish"
  },
  "keywords": ["karton", "element", "web-component", "framework"],
  "author": "Biensure",
  "license": "MIT",
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {}
}
